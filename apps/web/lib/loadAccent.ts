/**
 * Standard accent color used inside the load (event) modal — replaces
 * the per-asset header color for elements that should look the same
 * regardless of which asset's load you're editing (action buttons,
 * stat badges, schedule-type chips, document labels, etc.).
 *
 * Picked to match the "Add customer" / "Save as customer" affordance.
 */
export const LOAD_ACCENT          = '#0369a1';
export const LOAD_ACCENT_HOVER    = '#075985';
export const LOAD_ACCENT_BG       = '#f0f9ff';
export const LOAD_ACCENT_BG_HOVER = '#e0f2fe';
export const LOAD_ACCENT_BORDER   = '#bae6fd';
