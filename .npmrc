# Expo's package graph regularly has tight peer-dependency requirements that
# npm 7+ flags as conflicts even when the installed versions are fine. Without
# this, `npm install` errors with ERESOLVE on a fresh clone — same dance we
# kept fixing manually every time we touched expo deps.
legacy-peer-deps=true
